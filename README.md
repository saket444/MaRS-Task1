Task 1 taught me alot of things. The first included working with linux's ubuntu. While we did learn very basics during the very first programming classes, going deep into was kind of interesting. Was initially planning on dual booting instead, but the worry of losing data made me use the ubuntu terminal for windows. 
learnt various diffrent commands such as mkdir touch mv grep top date etc. While I still havent fully understood to use github, I understood it basics, and want to know more. Which is why I had to use manualy upload my files because I could not get the git push git commit commands to work through the terminal.
Learned to work with VS code. It has so many tools. The "run" button was not initially working at first. Then understood that it was using an extension which did not work. Figurred out what the difference between the output tab was and terminal, because initially i was using the output tab to check for my outputs.

Approches for medium dose:
1. I used linear algebra to find point A in the space to match with point A on the map.
2. Used dictionary. Read data from user and mapped to its corresponding translation from the dictionary.
3. Used basic shift formula(char+shift) and also handled the case of a loop from Z to A.
4. Condtional logic to identify to identify and "strip" noise from the Muchiko and Sanchiko data streams, ensuring only valid readings remained.
5. Used cumulative tracking to monitor the extension/retraction cycles, calculating mechanical wear through a mathematical formula.
6. Implemented a Breadth-First Search (BFS) algorithm(I was taught this in data science, though I am still not strong with it, I got help from an AI ). We treated the 11x11 arena as a coordinate grid, used a queue to explore possible moves, and skipped any "obstacle" coordinates to find the mathematically shortest route to the goal.

Overall this was a very fun experience. Along with some coding got to learn more about github, ubuntu, vm(virtual machine), dual booting, working with terminals, and VS code and maybe much more to come.
Thank you for this opportunity.
